import 'package:flutter/material.dart';

class FactionChooseScreen extends StatelessWidget {
  const FactionChooseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Выбор фракции')),
      body: const Center(child: Text('Заглушка: выбор фракции')),
    );
  }
}
