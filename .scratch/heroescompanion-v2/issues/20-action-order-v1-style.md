# 20 — UI: порядок действий как в v1 — только картинки, без названий и «чёрточек», зелёная подсветка при зажатии

**What to build:** Переделать виджет порядка действий на экране партии (тикет 07) в стиль первой версии (v1, `ResourceOrderImageList` из `legacy/v1-reference`): ячейки показывают только картинку действия — без названия и без иконки-«двух горизонтальных чёрточек» (`Icons.drag_handle`); при зажатии (длинном нажатии — начале перетаскивания) ячейка подсвечивается зелёным, как в v1.

- **UI (`lib/features/game/presentation/widgets/action_order_panel.dart`):**
  - Ячейка — картинка действия на всю ширину: `Image.asset(actionIconPaths[action])` (cover, скругление 12, высота ~55), карточка с отступами как в v1 (margin vertical 6, horizontal 16). Убрать из ячейки: текст названия действия (`actionNames` — «Дерево», «Перемещение» и т. д.) и иконку `Icons.drag_handle`.
  - Номера уровней 1–4 — как в v1: фиксированный слой поверх списка слева (`Positioned`), жирный белый шрифт (~30) с чёрной тенью. Пятая ячейка (невыбранное действие, семантика тикета 03) — без номера.
  - Зелёная подсветка при зажатии: `proxyDecorator` у `ReorderableListView` как в v1 — при перетаскивании ячейка подсвечивается зелёным свечением (boxShadow, цвет ~ `Color(0xFF4CAF50)`, blur/spread как в v1) с лёгким увеличением.
  - Фолбэк при отсутствии ассета: иконка действия (как сейчас); текст названия больше нигде не показывается. `actionNames` удалить, если нигде больше не нужен (сейчас используется только в этой панели).
- **Домен и сессию не трогаем:** `GameAction`, `ActionOrder`, `applyActionOrder` и уровни работают как сейчас; поведение перетаскивания не меняется.
- **Тесты:** обновить виджет-тесты экрана партии: в панели есть картинки и номера 1–4, нет названий действий и `Icons.drag_handle`; перетаскивание по-прежнему применяет порядок к сессии. Зелёную подсветку (`proxyDecorator`) автоматически не тестируем — проверяется вручную.

**Blocked by:** 07 (экран партии, панель порядка действий)

**Status:** ready-for-agent

- [ ] Ячейки панели — только картинки действий: без названий и без «двух горизонтальных чёрточек» (`drag_handle`)
- [ ] Номера уровней 1–4 поверх картинок в стиле v1 (белый жирный с тенью); пятая ячейка — без номера
- [ ] При зажатии (перетаскивании) ячейка подсвечивается зелёным, как в v1
- [ ] Перетаскивание применяет порядок к сессии (поведение не изменилось)
- [ ] Фолбэк без ассета — иконка действия
- [ ] Виджет-тесты: в панели нет названий действий и `drag_handle`; номера 1–4 есть; перетаскивание работает

## Comments
