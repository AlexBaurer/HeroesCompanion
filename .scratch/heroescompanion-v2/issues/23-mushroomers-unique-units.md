# 23 — Экран партии Гриболюдов: по одному воину каждого типа, сетка 3×4 вместо крутилок

**What to build:** У Гриболюдов (тикет 07) не может быть больше одного воина каждого типа — счётчики «всего» и «в бой» (тикет 07, колёсики `ResourceCounterWheel` в `ArmyBlockPanel`) не имеют смысла. У фракции 12 разновидностей воинов. Заменить ленту юнитов с крутилками на сетку 3 ряда × 4 колонки с меньшими картинками: тап по картинке подсвечивает юнита и добавляет его силу «в бой», повторный тап снимает подсветку и силу.

- **Данные и домен:**
  - В `Faction` (и JSON фракции) — новый флаг «по одному воину каждого типа» (например `uniqueUnits`): у Гриболюдов `true`, у остальных `false` (поле опциональное, по умолчанию `false`). Парсер (`faction_parser.dart`) и контракт данных (`faction_data_test.dart`) дополняются.
  - `GameSession` для `uniqueUnits`-фракций: общее число каждого юнита всегда `1` (в наличии), «в бой» — `0/1`; армия не «тратится». Новый метод (например `toggleDeployed(unitId)`): в бой `1` ↔ `0`. `setArmyTotal` для таких юнитов не уменьшает общее число ниже 1.
  - Сила: `totalArmyStrength` = Σ `unitPower` по всем 12 юнитам (константа для Гриболюдов); `deployedArmyStrength` = Σ `unitBattlePower` по заявленным «в бой» (каждый один раз). Существующие формулы работают, если `_armyTotal` = 1 и `_armyDeployed` = 0/1.
  - Граница раунда (`advanceRound`): для `uniqueUnits` «в бой» сбрасывается (юниты отзываются), но НЕ вычитается из общего числа (сейчас `_resetDeployedArmy` вычитает отправленных) — у Гриболюдов воины всегда доступны.
  - Модификаторы не меняются: toggle-модификаторы Гриболюдов (`+4` к силе Шляпника/Грибового/Лисидера) применяются через `unitPower`/`unitBattlePower` как раньше.
- **UI (`ArmyBlockPanel` в `lib/features/game/presentation/widgets/army_block_panel.dart`):**
  - Для `uniqueUnits`-фракций вместо горизонтальной ленты с колёсиками — сетка 3 колонки × 4 ряда (`GridView.count`/`Wrap`) на 12 юнитов в порядке данных фракции. Картинки юнитов меньше (ассет `assets/units/<id>.PNG`, тот же `errorBuilder` — серая плашка с именем; папка пока пустая), подпись имени.
  - Карточка юнита: тап → подсветка (рамка/оверлей цветом фракции из `colorFromHex`, можно галочку) и «в бой» = 1; повторный тап → подсветка и «в бой» убираются. Одновременно можно выбрать несколько юнитов (нет ограничения, кроме 12).
  - Бейджи «Сила армии: N» / «Сила в бой: N» сверху остаются (у Гриболюдов «Сила армии» — константа, т.к. все 12 в наличии по одному). Высота панели при необходимости корректируется под 4 ряда.
  - Остальные фракции — текущая лента с крутилками, без изменений.
- **Тесты:**
  - Домен/сессия: toggle «в бой» 0/1; сила в бой = Σ по заявленным; сила армии = Σ всех; граница раунда отзывает «в бой» без потребления (общее число остаётся 1); toggle-модификаторы продолжают менять силу в бой.
  - Парсер/контракт данных: новый флаг у Гриболюдов, отсутствует/по умолчанию у остальных.
  - Виджет (`game_screen_test.dart`): у Гриболюдов сетка 3×4 без крутилок; тап подсвечивает и добавляет силу «в бой», повторный тап убирает; бейджи силы обновляются; у других фракций лента с крутилками как раньше.

**Blocked by:** — (строится на 03 session-domain и 07 game-screen-ui, оба готовы)

**Status:** ready-for-agent

- [ ] Флаг `uniqueUnits` в данных/домене: JSON Гриболюдов, парсер, контракт
- [ ] Сессия: toggle «в бой» 0/1, общее число всегда 1, граница раунда без потребления
- [ ] UI: сетка 3×4 с подсветкой выбранных, без крутилок; остальные фракции не тронуты
- [ ] Сила армии/в бой пересчитывается от выбора (с учётом модификаторов)
- [ ] Тесты: домен (toggle, сила, граница раунда), данные, виджет (сетка, подсветка, переход)

## Comments

Открытые решения (заложены выше как предложение, реализатор может уточнить):
- Название флага и имя метода — рабочие (`uniqueUnits`, `toggleDeployed`).
- «Сила армии» у Гриболюдов — константа (все 12 в наличии) — бейдж оставлен информационным.
- Граница раунда: юниты отзываются из боя, но не потребляются.
