# 22 — Экран выбора фракции: описание раскрывается в плитке, окно фракции убрано

**What to build:** Убрать полноэкранное окно фракции (тикет 19, маршрут `/faction_preview/:factionName`) и перенести его содержимое на экран выбора фракции (тикет 12): тап по плитке больше не ведёт на отдельный экран — плитка раздвигается вниз с анимацией, под ней появляется краткое описание фракции (поле `description` из JSON, уже есть у всех 18 фракций — тикет 19) и кнопка «Выбрать», которая ведёт на экран партии (тикет 07) с выбранной фракцией.

- **Удаление окна фракции (тикет 19):** удалить `lib/features/main_menu/presentation/faction_preview_screen.dart`, маршрут `/faction_preview/:factionName` и его импорт из `lib/app/app_router.dart`, тест `test/features/main_menu/faction_preview_screen_test.dart`.
- **UI (`lib/features/main_menu/presentation/faction_choose_screen.dart`):**
  - Тап по плитке — раздвижение вниз с анимацией (например, `AnimatedSize`/`SizeTransition` вокруг панели); под плиткой появляется панель с описанием и кнопкой. Стиль панели и кнопки — как в окне тикета 19: полупрозрачная поверхность со скруглением; кнопка «Выбрать» акцентная, цвет фракции (`colorFromHex` + контрастный текст), подпись «Начать игру» заменяется на "Выбрать".
  - Одновременно раскрыта только одна плитка: тап по другой сворачивает раскрытую и раздвигает новую; повторный тап по раскрытой плитке сворачивает её.
  - Список из 18 плиток скроллится как раньше; раскрытая плитка раздвигает следующие за ней вниз.
  - Имя и фон плитки не меняются (тикет 12).
- **Переход в партию:** «Выбрать» — `context.push('/faction/<имя>')` (стек: выбор → партия, как после «Начать игру» окна 19: окно заменялось партией поверх выбора). «Назад» из партии (двойное нажатие, тикет 07) по-прежнему возвращает на выбор фракции.
- **Данные и домен не меняются:** `Faction.description` уже обязательное поле во всех 18 JSON (тикет 19) — только чтение.
- **Тесты:** удалить `faction_preview_screen_test.dart`; обновить `test/features/main_menu/faction_choose_screen_test.dart` (тап по плитке раскрывает панель: описание и «Выбрать»; одна раскрытая плитка; повторный тап сворачивает; «Выбрать» ведёт на экран партии), `test/features/game/game_screen_test.dart` и `test/smoke_test.dart` — переход в партию через раскрытие плитки вместо окна.

**Blocked by:** 19 (описание в JSON и стиль панели/кнопки), 12 (плитки выбора), 07 (экран партии)

**Status:** ready-for-agent

- [x] Окно фракции удалено: файл экрана, маршрут `/faction_preview/:factionName`, тест окна
- [x] Тап по плитке раздвигает её вниз с анимацией: описание и «Выбрать» появляются под плиткой
- [x] Одновременно раскрыта одна плитка; тап по другой переключает, повторный тап сворачивает
- [x] «Выбрать» ведёт на экран партии с выбранной фракцией; «назад» из партии — на выбор фракции
- [x] Виджет-тесты обновлены: раскрытие/сворачивание, одна раскрытая, переход в партию; тест окна удалён

## Comments

Реализовано: удалены `lib/features/main_menu/presentation/faction_preview_screen.dart`, маршрут `/faction_preview/:factionName` (импорт и `GoRoute` из `lib/app/app_router.dart`) и `test/features/main_menu/faction_preview_screen_test.dart`. В `lib/features/main_menu/presentation/faction_choose_screen.dart` `_FactionList` стал `StatefulWidget` с состоянием `_expandedFaction` (раскрыта одна плитка; тап по другой переключает, повторный — сворачивает); под плиткой — `AnimatedSize` (250ms, `Curves.easeInOut`, `Alignment.topCenter`): свёрнутая плитка — `SizedBox.shrink` (высота 0, места не занимает), раскрытая — панель `_ExpandedPanel`: полупрозрачная поверхность (`surface` 0.85, скругление 12, отступы 16/8), описание `Faction.description` и кнопка «Выбрать» (цвет фракции + `contrastingForeground`, стиль из окна тикета 19, подпись заменена). «Выбрать» — `context.push('/faction/<имя>')` — стек «выбор → партия»; двойное «назад» из партии (тикет 07) возвращает на выбор фракции, раскрытое состояние плитки сохраняется. Данные и домен не менялись. Тесты: `faction_choose_screen_test.dart` — раскрытие (описание + «Выбрать», без «Начать игру»/партии), одна раскрытая плитка с переключением и сворачиванием, «Выбрать» → партия; `game_screen_test.dart` и `smoke_test.dart` — переход через раскрытие плитки. Требуется финальный прогон `flutter analyze && flutter test` на машине со SDK.
